{
    "name": "SharedSpoons",
    "version": "0.0.1",
    "private": true,
    "scripts": {
      "installs": "cd frontendSim && npm install && cd ios && pod install"
    },
    "dependencies": {
    },
    "devDependencies": {
    },
    "engines": {
    }
  }
  